// Default runtime configuration for the dashboard. This file is overwritten during builds
// when environment variables are provided. It intentionally exports no modules so it can
// be consumed as a plain script tag before app.js.
window.__APP_DEFAULTS__ = window.__APP_DEFAULTS__ || {};
